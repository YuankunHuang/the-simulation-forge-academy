# 本地用 `npm run worker:dev` 联调 worker/ 时，复制这个文件为 .dev.vars（已在 .gitignore 里，不会被提交），
# 填一个和 treasure-box/.dev.vars 里 BOX_PASSPHRASE 相同的值即可（本地联调时两边各自跑自己的
# Worker，用同一个明文口令手动签一个 cookie 或直接在代码里临时放行都可以，不强求本地也走完整门禁）。
# 生产环境的 PORTAL_SESSION_SECRET 在 Cloudflare 控制台的 Worker → Settings → Variables and
# Secrets 里单独设置，和这个本地文件无关。
PORTAL_SESSION_SECRET=change-me-local-only
